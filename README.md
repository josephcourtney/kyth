# Kyth

Features
--------
- Serves a directory over HTTP.
- Injects a small client script into HTML pages.
- Forwards browser console output to the server terminal.
- Auto-reloads connected pages when watched assets change.
- Restarts the Python process when this script changes.
- Picks an unused HTTP port automatically.
- Opens the app in the default browser after startup.

